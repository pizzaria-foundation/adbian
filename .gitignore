# Rust build output. Each device crate is its own workspace root, so it has a target/ of its own.
/target/
**/device/target/
**/examples/target/

# Per-app build directories: objects, ELF, E32, resources, .sis (all regenerated by the SDK's
# tools/epoc). A built E32 image embeds a signing cert and SID — not something to publish.
**/build/

# Python
__pycache__/
*.pyc
